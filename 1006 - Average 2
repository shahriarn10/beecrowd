#include <stdio.h>
 
int main() {
 
    float x, y, z, result, result2;
    scanf("%f%f%f", &x, &y, &z);
    result=x*2+y*3+z*5;
    result2=result/10.0;
    printf("MEDIA = %.1f\n", result2);
    return 0;
}
